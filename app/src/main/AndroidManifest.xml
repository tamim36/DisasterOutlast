<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.disasteroutlast">

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".addpost.BasicActivtity"></activity>
        <activity android:name=".addpost.ReviewActivity" />
        <activity android:name=".addpost.PostActivity" />
        <activity android:name=".addpost.Post_inner_part" />
        <activity android:name=".addpost.MainPost" />
        <activity android:name=".disasternews.MainDisaster" />
        <activity android:name=".loginregister.Homepage" />
        <activity android:name=".loginregister.RegisterActivity" />
        <activity android:name=".loginregister.PasswordActivity" />
        <activity android:name=".loginregister.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".weatherinfo.WeatherMain" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>